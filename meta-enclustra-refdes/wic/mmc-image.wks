# Description: Creates a partitioned SD card image. Boot files
# are located in the first vfat partition. Rootfs will be in second ext4 partition.

part --source bootimg-partition --fstype=vfat --label boot --active --align 4 --fixed-size 100M
part --source rootfs --fstype=ext4 --label root --align 4 --fixed-size 1G
